//import { User } from '../types/user';
//import apiInstance from './apiInstance';

/*export const getCurrentUserInfo = async (): Promise<User | null> => {
  try {
    const userInfoRes = await apiInstance.get('/profile');
    console.log(userInfoRes.status);
    return userInfoRes.status ? userInfoRes.data() : null;
  } catch (e) {
    console.error(e);
    return null;
  }
};*/
export const getCurrentUserInfo = async () => {
  /*return {
    username: 'string',
    userInfo: {
      name: 'hi',
    },
  };*/
  return null;
};
